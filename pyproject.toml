[project]
name = "rag-with-kg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.2.0",
    "torch>=2.9.1",
    "weaviate>=0.1.2",
]
